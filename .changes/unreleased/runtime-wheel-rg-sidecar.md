---
category: Changed
---
- README Runtime acquisition (Route A) documents the runtime wheel's ripgrep `-rg` sidecar: the bundled default config does not use it, but a cordis config that mounts the fs-search tool needs it beside the executable (copy it along when relocating the executable).
