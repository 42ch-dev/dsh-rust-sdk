---
category: Changed
---
- Updated the wire-protocol-parity knowledge file to replace a stale three-platform enumeration (linux-x64 / linux-arm64 / macos-arm64) with a pointer to the README's `Platform support & MSRV` section, so the runtime-bin companion-crate guidance no longer drifts when the published wheel matrix changes.
