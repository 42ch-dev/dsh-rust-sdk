---
category: Fixed
---
- Corrected the changelog-fragment format example in `docs/release.md` and `.changes/unreleased/README.md` to use `Config::dsh_bin` (the current field name) instead of the pre-rename `Config::runtime_bin`.
