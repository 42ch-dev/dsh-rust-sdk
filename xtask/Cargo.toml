[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
description = "Release automation (fragment changelog + version) for deepseek-harness-sdk"
publish = false

[dependencies]
anyhow = "1"
semver = "1"
toml_edit = "0.22"

[dev-dependencies]
tempfile = "3"
